version: 2

macros:
  - name: extract_product_id
    description: Extracts the product_id from the product url
    docs:
      show: true
    arguments:
      - name: column_name
        type: str
        description: Name of the product url from stg_events
